# CLAUDE.md

このプロジェクトは Claude Code / Codex / Gemini CLI を切り替えて開発する。
AI間の引き継ぎは **`HANDOFF.md` と Git の状態だけ**で成立させる。会話履歴に依存しない。

## 運用ルール

1. **作業開始時**に必ず `HANDOFF.md` を読み、`git status` と `git diff` を確認する
2. ユーザーが「**続き**」とだけ言ったら、`HANDOFF.md` の「次にやること」と Git 差分から直前の状態を判断し、確認を挟まずそのまま続行する
3. 重要な進捗・仕様決定・発生した問題・未完了事項・次にやることが変わったら、**その場で** `HANDOFF.md` を更新する（作業の最後にまとめてではなく）
4. `HANDOFF.md` は**会話履歴を一切知らない他のAIが即座に続行できる**内容に保つ。前提・パス・コマンド・判断理由を省略しない
5. 他のAI（Codex / Gemini）が加えた変更も、Git 差分と `HANDOFF.md` から把握して引き継ぐ。自分の記憶と食い違ったら **Git の実体を正**とする
6. 引き継ぎのためだけに新しいドキュメントや仕組みを増やさない。状態は `HANDOFF.md` 1枚に集約する

## プロジェクト固有のルール

`HANDOFF.md` の §3（アーキテクチャ）と §4（守るべきルール）に従う。特に：

- **数値は `raw/*.csv` 由来のみ**。推定・補完・丸めによる捏造は禁止。出せない値は空ける
- 秘匿記号 `X`（秘匿）/ `...`（非公表）/ `-`（該当なし）を潰さない
- `scripts/build_data.py` の整合性 assert を無効化しない
- クライアントJSを足さない（`output: 'export'` の完全静的を維持）
- Next 15 のため `params` は Promise。ページは `async` にして `await params` する
- `raw/` を更新したら `npm run data` を回し、`data/dataset.json` も一緒にコミットする

## コミット

- 完了した作業単位ごとにコミットする。`HANDOFF.md` の更新は同じコミットに含める
- メッセージは日本語1行サマリ＋必要なら箇条書き
